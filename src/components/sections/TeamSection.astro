---
import SectionHeading from "../common/SectionHeading.astro";

const base = import.meta.env.BASE_URL;
---

<section id="team" class="bg-surface-dim py-14 sm:py-20">
  <div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="animate-fade-up" data-animate>
      <SectionHeading titleEn="TEAM" titleJa="代表プロフィール" />
    </div>

    <div class="flex flex-col sm:flex-row items-center sm:items-start gap-8 mt-8 animate-fade-up" data-animate>
      <!-- Photo -->
      <div class="shrink-0">
        <img
          src={`${base}ceo.jpg`}
          alt="齋本 竜也"
          class="w-32 h-32 sm:w-40 sm:h-40 rounded-2xl object-cover object-top"
          loading="lazy"
        />
      </div>

      <!-- Profile -->
      <div class="flex-1 text-center sm:text-left">
        <h3 class="text-2xl sm:text-3xl font-black text-text-primary">
          齋本 竜也
        </h3>
        <p class="font-heading text-xs tracking-wider text-text-secondary uppercase mt-1">
          TATSUYA SAIMOTO
        </p>
        <p class="text-sm text-navy font-bold mt-2">代表取締役</p>

        <p class="text-sm text-text-secondary leading-relaxed mt-4">
          2009年、関西学院大学卒業後、上場保険代理店に入社。11年半にわたり人事・経理・総務・マーケティング・コンプライアンスなど多様な実務を経験。2020年、株式会社SAIMONを創業。AI研修事業を主軸に、最新技術を実装に落とし込み企業の変革を加速させている。
        </p>

        <!-- Career keywords -->
        <div class="flex flex-wrap justify-center sm:justify-start gap-2 mt-4">
          <span class="text-[11px] font-bold text-teal bg-teal/5 border border-teal/15 px-3 py-1 rounded-full">10年超の現場経験</span>
          <span class="text-[11px] font-bold text-teal bg-teal/5 border border-teal/15 px-3 py-1 rounded-full">好奇心と行動力</span>
          <span class="text-[11px] font-bold text-teal bg-teal/5 border border-teal/15 px-3 py-1 rounded-full">顧客の夢を実現</span>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  const teamElements = document.querySelectorAll<HTMLElement>('#team [data-animate]');
  if (teamElements.length > 0) {
    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            entry.target.classList.add('animate-visible');
            observer.unobserve(entry.target);
          }
        });
      },
      { threshold: 0.1 }
    );
    teamElements.forEach((el) => observer.observe(el));
  }
</script>
