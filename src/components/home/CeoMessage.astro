---
const base = import.meta.env.BASE_URL;
---

<section class="py-24 sm:py-32 bg-surface">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Section heading -->
    <div class="text-center mb-16 animate-fade-up" data-animate>
      <p class="text-teal text-sm font-bold tracking-[0.3em] uppercase mb-3">Message</p>
      <h2 class="text-3xl sm:text-4xl font-black text-text-primary">
        代表メッセージ
      </h2>
      <div class="mt-4 w-12 h-1 bg-teal mx-auto rounded-full"></div>
    </div>

    <div class="grid lg:grid-cols-2 gap-12 lg:gap-16 items-center max-w-5xl mx-auto">
      <!-- Text side -->
      <div class="animate-fade-up" data-animate style="transition-delay: 0.1s;">
        <blockquote class="text-text-primary text-base sm:text-lg leading-loose">
          <p>
            2009年から保険代理店で人事・Web・経営企画に携わり、中小企業の経営課題に向き合ってきました。そこで感じたのは、テクノロジーの力で企業はもっと面白くなれるということ。2020年10月、その想いを形にするために株式会社SAIMONを設立しました。「面白い」を実現させる——それが私たちの使命です。
          </p>
        </blockquote>

        <div class="mt-8 flex items-center gap-4">
          <div class="w-12 h-0.5 bg-teal"></div>
          <div>
            <p class="text-text-primary font-black text-lg">齋本 竜也</p>
            <p class="text-text-secondary text-sm">代表取締役</p>
          </div>
        </div>

        <div class="mt-8">
          <a
            href={`${base}philosophy/`}
            class="inline-flex items-center gap-2 text-navy font-bold hover:text-teal transition-colors duration-200 group"
          >
            続きを読む
            <svg class="w-4 h-4 transform group-hover:translate-x-1 transition-transform duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
            </svg>
          </a>
        </div>
      </div>

      <!-- Decorative side -->
      <div class="relative flex items-center justify-center animate-fade-up" data-animate style="transition-delay: 0.2s;" aria-hidden="true">
        <div class="relative w-full max-w-sm aspect-square">
          <!-- Background shapes -->
          <div class="absolute inset-4 rounded-3xl bg-gradient-to-br from-navy/5 to-teal/10 rotate-3"></div>
          <div class="absolute inset-8 rounded-3xl bg-gradient-to-tl from-teal/5 to-navy/10 -rotate-3"></div>

          <!-- Large quotation mark -->
          <div class="absolute inset-0 flex items-center justify-center">
            <svg class="w-48 h-48 text-navy/10" viewBox="0 0 100 100" fill="currentColor">
              <path d="M30 60c-8.284 0-15-6.716-15-15 0-8.284 6.716-15 15-15 0.256 0 0.51 0.006 0.763 0.02C35.152 17.667 45.388 10 55 10v10c-5.523 0-10 7.477-10 15h5c8.284 0 15 6.716 15 15s-6.716 15-15 15H30zm40 0c-8.284 0-15-6.716-15-15 0-8.284 6.716-15 15-15 0.256 0 0.51 0.006 0.763 0.02C75.152 17.667 85.388 10 95 10v10c-5.523 0-10 7.477-10 15h5c8.284 0 15 6.716 15 15s-6.716 15-15 15H70z" transform="translate(-10, 15) scale(0.8)" />
            </svg>
          </div>

          <!-- Geometric accents -->
          <div class="absolute top-0 right-0 w-16 h-16 border-2 border-teal/20 rounded-xl rotate-12"></div>
          <div class="absolute bottom-4 left-4 w-8 h-8 bg-teal/15 rounded-lg -rotate-6"></div>
          <div class="absolute top-1/2 left-0 w-2 h-2 bg-navy/30 rounded-full"></div>
          <div class="absolute bottom-1/4 right-8 w-3 h-3 border border-navy/20 rounded-full"></div>

          <!-- Central tagline -->
          <div class="absolute inset-0 flex items-center justify-center">
            <p class="text-2xl sm:text-3xl font-black text-navy/15 tracking-wider text-center leading-relaxed select-none">
              面白いを<br />実現させる
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  const ceoElements = document.querySelectorAll<HTMLElement>('section:has(.max-w-5xl) [data-animate]');
  if (ceoElements.length > 0) {
    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            entry.target.classList.add('animate-visible');
            observer.unobserve(entry.target);
          }
        });
      },
      { threshold: 0.1 }
    );
    ceoElements.forEach((el) => observer.observe(el));
  }
</script>
