---
interface Props {
  class?: string;
  as?: string;
}

const { class: className = "", as: Tag = "div" } = Astro.props;
---

<Tag class:list={["max-w-7xl mx-auto px-4 sm:px-6 lg:px-8", className]}>
  <slot />
</Tag>
